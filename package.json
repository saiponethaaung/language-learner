{
  "private": true,
  "scripts": {
    "test": "turbo run test",
    "build": "turbo run build",
    "dev": "export TZ=UTC && turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "proto": "make proto",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "turbo": "^2.3.4"
  },
  "pnpm": {
    "overrides": {
      "cross-spawn": ">=7.0.5"
    }
  },
  "engines": {
    "node": "v23.3.0"
  },
  "packageManager": "pnpm@9.15.4"
}